import { SEED_PARTICIPANTS } from './mockDatabase';
import { Participant } from '../types/participant';

/**
 * PARTICIPANT MASTER DATABASE (MOCK)
 * Re-exports the unified seed participants list.
 */
export const MOCK_PARTICIPANTS: Participant[] = SEED_PARTICIPANTS;
